apiVersion: apps/v1
kind: Deployment
metadata:
  name: live-server
  labels:
    app: live-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: live-server
  template:
    metadata:
      labels:
        app: live-server
    spec:
      containers:
        - name: live-server
          image: christer10/live_server:latest
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
